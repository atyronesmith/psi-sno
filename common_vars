#!/bin/bash

PROJECT_NAME="${PROJECT_NAME:-"nfv-sno"}"
PROJECT_CIDR=${PROJECT_CIDR:-"11.0.0.0/16"}
PROJECT_DOMAIN="${PROJECT_DOMAIN:-nfv.com}"
PROJECT_CLUSTER="${PROJECT_CLUSTER:-psi}"

PSI_NETWORK=${PSI_NETWORK,-provider_net_shared_3}

SNO_ISO_IMAGE=${SNO_ISO_IMAGE:-rhcos-live.iso}
SNO_FLAVOR=${SNO_FLAVOR:-g.memory.xxl}
SNO_VOLUME_SIZE=${SNO_VOLUME_SIZE:-120}
SNO_IP_OFF=${SNO_IP_OFF:-100}

OCP_VERSION=${OCP_VERSION:-"4.18.10"}