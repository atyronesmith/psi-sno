apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml

components:
  - networking
  - operators
  - storage

commonLabels:
  app.kubernetes.io/part-of: openstack