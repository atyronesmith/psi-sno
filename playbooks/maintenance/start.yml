---
- name: Start Playbook
  hosts: localhost
  gather_facts: false
  connection: local
  vars_files:
    - ../../inventory/group_vars/all.yml

  tasks:
    - name: Read project metadata using iso-builder role
      ansible.builtin.include_role:
        name: openshift/iso-builder
        tasks_from: read_metadata

    # TODO: Implement start_cluster tasks or use appropriate role
    - name: Start cluster tasks placeholder
      ansible.builtin.debug:
        msg: "Start cluster functionality needs to be implemented"
