- name: Deploy a SNO environment in PSI
  hosts: localhost
  gather_facts: false
  vars_files:
    - common_vars.yml

  tasks:
    - name: Read project metadata
      ansible.builtin.include_tasks: tasks/read_metadata.yml

    - name: Get server FIP for cluster
      ansible.builtin.include_tasks: tasks/get_server_fip.yml
